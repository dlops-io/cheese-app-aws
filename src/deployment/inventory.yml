all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: "/secrets/ac215_2024.pem" ## Change this
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'
    # aws_access_key: "{{ lookup('env','AWS_ACCESS_KEY_ID') }}"
    # aws_secret_key: "{{ lookup('env','AWS_SECRET_ACCESS_KEY') }}"
    # aws_account_id: "{{ lookup('env','AWS_ACCOUNT_ID') }}"
    aws_access_key: "{{ aws_access_key }}"  # Changed to use extra-vars
    aws_secret_key: "{{ aws_secret_key }}"  # Changed to use extra-vars
    aws_account_id: "{{ aws_account_id }}"
    aws_application_credentials: "/secrets/deployment-ac215-user_accessKeys.csv" ## Change this

    aws_region: "us-east-1"
    # aws_vpc: "vpc-xxxxxxxx"
    # aws_subnet: "subnet-xxxxxxxx"
    # aws_security_group: "sg-xxxxxxxx"
    aws_instance_type: "t2.large"
    aws_keypair_name: "ac215_2024"  ## Change this 
    aws_ami_id: "ami-0e86e20dae9224db8"  
    docker_network_name: "appnetwork"
    machine_instance_name: "cheese-app-demo"
    machine_disk_size: 100
    persistent_disk_name: "cheese-app-demo-disk"
    persistent_disk_size: 50
    aws_instance_name: "cheese-app-demo"
    nextauth_secret: "fygW6/oAA4faDvyCYCTFuaQ4zRVeeC2DlZZ8NWv1hJY="
    nextauth_url: "http://{{ inventory_hostname }}"

  children:
    appserver:
      hosts:
        3.93.213.99:
